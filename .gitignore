# machine-local additions — not committed
local.star

# resolved dependency lockfile — regenerated by meowctl sync
deps.lock

# runtime state files generated by meowctl
rollback.jsonl
state.toml
installed.lock

# macOS
.DS_Store
